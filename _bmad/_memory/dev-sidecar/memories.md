# Memorie — Amelia — Senior Software Engineer (Fase 4 Implementation)

_Fatti durevoli e decisioni apprese durante il progetto HostPilot. Un fatto per voce, con data. Aggiornare via PR. Non duplicare `docs/project-context.md`._

<!-- Esempio:
- 2026-07-24 — <fatto appreso e perché conta>.
-->

- 2026-07-24 — Story 1.1 consegnata: monorepo `backend/`+`frontend/`, kernel `core`, CI. Il backend usa **uv** (non pip come il template): `uv sync --group dev`, `uv run pytest`. Test DB su PostgreSQL 18 reale via `HOSTPILOT_TEST_DATABASE_URL` (locale: Docker porta 54329); in CI `HOSTPILOT_TEST_DB_REQUIRED=1` impedisce skip silenziosi.
- 2026-07-24 — Contratto API: `backend/openapi.json` e `frontend/lib/api/schema.d.ts` sono **committati** e la CI (`api-contract`) fallisce se divergono dal codice. Dopo ogni modifica agli endpoint: `uv run python scripts/export_openapi.py` + `npm run generate:api`.
- 2026-07-24 — Catalogo `core/events.py`: registrare i tipi evento/job nella Story che li emette per la prima volta (catalogo di produzione parte vuoto; i test usano istanze `Catalog()` locali).
- 2026-07-24 — `eslint-config-next` 16 è flat-native: si importa da `eslint-config-next/core-web-vitals` e `/typescript`, senza FlatCompat (il template Next 15 va adattato).
- 2026-07-24 — shadcn/ui non ancora inizializzato (previsto con la Story 1.3, prima UI reale); e2e Playwright presente ma fuori dalla CI PR fino alla 1.3.
- 2026-07-24 — Story 1.2: FastAPI 0.139 include i router in modo **lazy** (`_IncludedRouter`): per ispezionare le route usare `route.effective_route_contexts()` (vedi `tests/test_auth_convention.py`). Il TestClient va creato con `base_url="https://testserver"` o il cookie Secure non viene mai rinviato.
- 2026-07-24 — Auth: ogni endpoint nuovo NON pubblico deve dichiarare `CurrentHost` (da `app.identity.deps`) — la guardia `test_auth_convention.py` fallisce altrimenti e va aggiornata la allowlist SOLO per endpoint davvero pubblici. `host_id` sempre dalla sessione.
- 2026-07-25 — Regola FIX-FORWARD attivata da Fahad: preparare un fix-batch è autonomo (branch `fix/<epic>-<batch>`, PR separata, verdetto di Murat), accettare (merge) è umano. Scope dei batch RIGIDO: solo i finding elencati.
- 2026-07-25 — Pattern per handler outbox/job: l'invocazione è dentro `session.begin_nested()` (SAVEPOINT per item, G-1) — un handler che scrive e poi solleva non lascia scritture parziali. I nuovi handler restano idempotenti E le loro scritture devono passare dalla sessione ricevuta, mai da sessioni proprie.
- 2026-07-25 — Debiti registrati da Murat per story future: G-3 (guardia strutturale tenant-scoping `host_id`) va consegnata CON la Story 1.4; G-5 (purge sessioni scadute come job + rate-limit login) resta proposta P2.
- 2026-07-25 — Story 1.3: `get_settings()` è lru_cache-ata e `app.main` la chiama a import-time (CORS) — nei test l'URL del DB va impostato PRIMA di ogni import di `app.*`: il conftest lo fa a modulo-import + `cache_clear()` nella fixture. Non rimuovere quelle righe.
- 2026-07-25 — Frontend: vitest senza `globals: true` → l'auto-cleanup di Testing Library non si attiva; è agganciato in `vitest.setup.ts` (afterEach(cleanup)). CLDR it-IT: separatore migliaia solo da 5 cifre ("1234,56 €"). shadcn/ui ancora NON inizializzato: attivarlo alla prima story con form complessi (1.4), è il seed ratificato G3-1.
- 2026-07-25 — Regola di gate aggiornata da Murat: dalla Story 1.3 il suo verdetto PRECEDE il merge umano — consegnare sempre con menzione a lui e non aspettarsi merge prima del verdetto.
- 2026-07-25 — Story 1.4: G-3 chiuso con `tests/test_tenancy_convention.py` — ogni NUOVO modulo di dominio è scoperto automaticamente: i suoi repository DEVONO avere `host_id` in ogni metodo pubblico e le sue tabelle `host_id` NOT NULL+FK (allowlist solo outbox/job/host). C1 chiuso: job CI `e2e` avvia backend reale via webServer Playwright (migrazioni+uvicorn, DB `hostpilot_e2e`, cookie secure=false SOLO lì) + axe serious/critical=0.
- 2026-07-25 — Alembic: `op.create_table` con colonna Enum CREA da sé il tipo Postgres — mai chiamare anche `enum.create(bind)` (DuplicateObject). Per `op.add_column` invece serve la create esplicita (vedi migrazione 0003 vs 0004).
- 2026-07-25 — Story 1.5: le tabelle di RIFERIMENTO condivise (anagrafica ISTAT, `config_normativa`) sono in allowlist esplicita in `test_tenancy_convention.py`, con un test che verifica che non acquisiscano mai un legame con `host`. `test_auth_convention.py` accetta ora due protettori: `CurrentHost` (sessione) o `require_admin_token` (endpoint `/interno`). Un modulo nuovo di dominio resta comunque sorvegliato: non aggiungerlo alle allowlist senza motivazione scritta.
- 2026-07-25 — G-5 chiuso. Worker: l'entrypoint è ora `python -m app.worker` (livello applicativo) — importa i moduli che registrano handler/subscriber e fa il bootstrap dei job periodici, poi cede a `app.core.worker` che resta il ciclo generico. `core` non deve MAI importare moduli di dominio (AD-1): ogni nuovo handler si registra aggiungendo l'import in `app/worker.py`.
- 2026-07-25 — Periodicità durevole senza scheduler esterno: l'handler si RIPROGRAMMA alla fine di ogni esecuzione (`_riprogramma`) e un bootstrap idempotente (`assicura_purge_periodico`) rimette in coda il ciclo se manca — così un restart non lo perde e non si creano duplicati.
- 2026-07-25 — Rate-limit login: tabella `tentativo_login` SENZA `host_id` (si scrive prima di sapere se l'account esiste: legarla all'Host rivelerebbe quali email sono registrate). È in allowlist `TABELLE_PRE_AUTENTICAZIONE` di `test_tenancy_convention.py`, sorvegliata dal test che vieta legami con `host`. Il freno si applica PRIMA di verificare le credenziali e vale anche per email inesistenti, altrimenti la differenza di comportamento diventa enumerazione.
- 2026-07-25 — F-1 chiuso: il cap Strutture è serializzato per Host con `pg_advisory_xact_lock(namespace, hashtext(host_id))` preso PRIMA del conteggio (`StrutturaRepository.blocca_creazioni_dell_host`). Lock consultivo legato alla transazione: si rilascia da solo a commit/rollback e non tocca tabelle di altri moduli (AD-18). Namespace 1001 riservato a questo uso: se servirà un altro advisory lock, usare un namespace diverso.
- 2026-07-25 — Riprodurre un TOCTOU nei test: 2 thread non bastano (l'interleaving spesso non capita), 8 thread con `threading.Barrier` lo colgono in modo affidabile. Evitare di strumentare la finestra critica con un barrier interno: con un fix basato su lock quel barrier va in deadlock.
- 2026-07-25 — F-3 chiuso: `secrets.compare_digest` va chiamato sui BYTE (`.encode()`), mai su stringhe che possono contenere non-ASCII — le intestazioni HTTP sono decodificate latin-1 e il confronto su str solleverebbe TypeError (500 al posto di 403).
- 2026-07-25 — Story 1.6: il Regime fiscale è una FUNZIONE del conteggio Strutture, non uno stato (AD-12) — un test vieta colonne `regime*`/`fiscal*` sulle entità. L'unico stato persistito è la conferma di lettura del pannello (`regime_lettura`), e il rientro sotto soglia la azzera: senza quello, dopo ridiscesa+risalita il pannello non ricompariva (UJ-4 edge).
- 2026-07-25 — Frontend: quando un valore derivato ha una sua query (es. `regime-fiscale` che dipende dal conteggio Strutture), OGNI mutazione sulla sorgente deve invalidare anche quella cache — centralizzato in `invalidaStruttureERegime`. Bug trovato solo dall'e2e: i test di componente con hook mockati non lo vedono.
- 2026-07-25 — Playwright: `window.confirm` è rifiutato di default; accettarlo con `page.once("dialog", d => d.accept())` prima del click, altrimenti il test fallisce a valle in modo fuorviante.
- 2026-07-25 — Validità temporale (config_normativa): una nuova riga chiude le aperte con `valido_dal <= nuovo` (pari decorrenza inclusa: delibera ri-emessa) e la risoluzione ordina per `valido_dal DESC, creato_il DESC`. Regola generale per ogni futura tabella a validità temporale: chiusura + tiebreaker deterministico, mai affidarsi all'ordine di ritorno del DB.
- 2026-07-25 — Anagrafica: NON committare codici ISTAT dei Comuni inventati. Le 20 Regioni (codici stabili) sono seedate nella migrazione 0005; i Comuni si importano dal CSV ufficiale con `python -m app.config_normativa.importa_comuni <file.csv>` (upsert idempotente). Nei test si usano codici sintetici marcati come tali.
- 2026-07-25 — Frontend: un `<datalist>` dentro la `<label>` finisce nel testo accessibile del campo e rompe `getByLabelText` (oltre all'a11y): tenerlo fuori e usare `htmlFor`/`id` espliciti.
- 2026-07-25 — Il repo ha **SonarCloud** come check obbligatorio sulle PR (quality gate su New Code) oltre a GitHub Actions: dopo ogni push controllare ENTRAMBI. Regole che ci hanno bloccato: action di terze parti da pinnare al commit SHA (S7637), `uv sync`/`npm ci` da eseguire senza esecuzione di script (`--locked --no-build --no-install-project`, `--ignore-scripts`), mai `npx` in CI (usare `npm exec --no --`), `response_model` FastAPI ridondante col return type (S8409), `role="status"` → `<output>` (S6819), props React `Readonly` (S6759).
- 2026-07-25 — In CI il progetto backend NON è installato nel venv (`--no-install-project`): i comandi girano con cwd=backend e `uv run --no-sync`. Uno script in `scripts/` deve aggiungersi la dir backend a `sys.path` (python mette in path la dir dello script, non la cwd) — vedi `scripts/export_openapi.py`.
- 2026-07-25 — Dopo aver aggiunto una dipendenza npm, rigenerare e committare `package-lock.json` verificandolo con `npm ci` (non basta `npm install`): le opzionali multipiattaforma `@emnapi/*` possono restare disallineate e far fallire tutti i job che installano il frontend.
- 2026-07-25 — Playwright: vietato importare da un file `*.spec.ts` a un altro — gli helper condivisi vanno in file non-spec (es. `e2e/axe-utils.ts`). La nuova regola eslint react-hooks vieta setState sincrono negli effect: per inizializzare form da dati async usare componente figlio con `key` + useState(prop).
- 2026-07-26 — Story 2.1 (primo codice di rete in USCITA del progetto). Il client HTTP verso i Feed si INIETTA come parametro del service (`esegui_sync(..., client=...)`), mai come import globale: è la condizione perché i test sostituiscano il confine senza mockare il service. In test la rete si stub-a AL TRASPORTO con `tests/server_feed.py` (ThreadingHTTPServer su 127.0.0.1 che sa fare redirect, Content-Length bugiardo, chiusura anticipata, silenzio).
- 2026-07-26 — GS-1 attiva: `tests/conftest.py::isolamento_di_rete` è `autouse` e blocca connect/create_connection/getaddrinfo fuori dal loopback. La sua eccezione deriva da **BaseException** di proposito — `valida_destinazione` converte `OSError` in «destinazione non ammessa» e il client converte gli errori di rete in «URL non raggiungibile»: con `Exception` la guardia verrebbe assorbita e un test senza risolutore iniettato passerebbe per il motivo sbagliato.
- 2026-07-26 — `Base.metadata.tables` contiene SOLO le tabelle dei moduli importati: una guardia che li legge prima non fallisce, tace. `test_tenancy_convention.py` era cieco alle tabelle di un modulo non raggiungibile da `app.main`. Da qui ogni guardia sul modello passa da `tests/modello.py::carica_modelli()` (importa anche `app.core.jobs`/`outbox`, che dichiarano tabelle fuori dai moduli di dominio).
- 2026-07-26 — `get_settings()` è lru_cache-ata: un test che cambia un env var lascia il valore in cache anche dopo il ripristino di `monkeypatch`, e il test dopo vede un'impostazione che non ha chiesto. Fixture `autouse` `configurazione_pulita` che svuota la cache DOPO ogni test (non basta farlo dentro il test).
- 2026-07-26 — Idempotenza dell'import: `INSERT ... ON CONFLICT (feed_id, ical_uid) DO UPDATE` in UNA istruzione, zero pre-check applicativi. `RETURNING (xmax = 0)` distingue inserita da aggiornata — senza, il conteggio «importate» è una stima. Rosso verificato: con check-then-write e senza il UNIQUE, 8 thread producono 8 righe duplicate.
- 2026-07-26 — «Job prioritario» si ottiene con `due_at = utcnow()` (già scaduto), NON aggiungendo una colonna `priority` a `job`: i cicli periodici hanno sempre `due_at` nel futuro, quindi l'on-demand li precede senza toccare `core` per il bisogno di un solo dominio.
- 2026-07-26 — Un handler di job non fa MAI `commit`: gira dentro `session.begin_nested()` (SAVEPOINT per item, G-1) e un commit interno lo scavalcherebbe. Corollario in `calendario`: `esegui_sync` non solleva sugli errori di rete/feed — li REGISTRA come `sync_run` fallito, altrimenti il rollback del savepoint cancellerebbe la traccia insieme all'eccezione.
- 2026-07-26 — Invariante di dato di AD-4/AD-19 imparato a caro prezzo (E2-G3, R2-C): `rimossa_dal_feed` si applica SOLO dopo un parse completo e validato (`END:VCALENDAR` presente, nessun VEVENT non chiuso), e `uid_presenti` deve includere anche gli uid degli eventi MALFORMATI — un VEVENT con UID ma senza DTEND è nel feed, escluderlo marcherebbe rimossa una Prenotazione viva. Con l'implementazione ingenua una risposta troncata marca `rimossa_dal_feed` l'intero calendario.
- 2026-07-26 — Politica di uscita di rete (NFR-17): denylist sull'indirizzo RISOLTO, tutti gli indirizzi restituiti dal DNS devono passare (un round-robin con un indirizzo interno sarebbe un bypass), redirect NON seguiti dal client HTTP ma un hop alla volta con rivalidazione, cap di dimensione applicato in streaming E sul `Content-Length` dichiarato. Oltre a loopback/privati/link-local servono CGNAT `100.64/10`, `192.0.0.0/24`, `100.100.100.200` (metadati Alibaba), NAT64 `64:ff9b::/96` e lo srotolamento degli IPv4-mapped. Il rifiuto usa la STESSA categoria d'errore di «irraggiungibile»: distinguerle è un canale per mappare la rete interna. Limite noto non chiuso: DNS rebinding (servirebbe il pinning dell'indirizzo nel trasporto).
- 2026-07-26 — `sa.Enum` condiviso da due tabelle nella stessa migrazione: il PRIMO uso dichiara i valori (e crea il tipo), i successivi usano `sa.Enum(name="...", create_type=False)` — idioma già in migrazione 0005. Nei modelli, una sola istanza `Enum` riusata dalle due colonne.
- 2026-07-26 — A2 chiusa: `.github/workflows/ci.yml` ha ora `permissions: contents: read` a livello di workflow. Un job che dovrà scrivere dichiarerà il permesso al proprio livello.
- 2026-07-26 — TRAPPOLA SQLAlchemy che costa un P0: `colonna.not_in([])` su una lista VUOTA non e' «nessuna riga», e' `(col NOT IN (NULL) OR (1 = 1))` — vero per OGNI riga. Una UPDATE con quel filtro degenera in «tutte». Ogni `in_`/`not_in` costruito da una lista calcolata a runtime vuole una guardia esplicita sul caso vuoto, nel punto piu' basso possibile (il repository), oltre alla decisione a monte.
- 2026-07-26 — Corollario del P0: una guardia «nessun evento» non copre «nessun evento IDENTIFICABILE». Nel sync iCal si decide su `uid_presenti`, non su `analizzato.eventi`: un VCALENDAR chiuso i cui VEVENT non hanno UID passa il parser, ha eventi, e arriva alla riconciliazione con l'insieme di confronto vuoto.
- 2026-07-26 — Un handler di job gira dentro `session.begin_nested()`: QUALUNQUE eccezione non catturata annulla anche la riga di traccia che l'handler ha appena scritto. Su un loop che elabora contenuto di terze parti la cattura va larga — ma SOLO attorno alla parte PURA (il normalizzatore): allargarla attorno a una scrittura DB farebbe proseguire su una transazione abortita. I limiti di lunghezza delle colonne si applicano a monte, nel codice puro, senza lasciare sbagliare il DB.
- 2026-07-26 — `httpx.Timeout` e' solo per-operazione: un portale che sgocciola un byte appena dentro il timeout di lettura non fa scattare nulla e puo' tenere la connessione per mesi. Serve una deadline monotona calcolata UNA volta per fetch, controllata per hop e dentro il ciclo di `iter_bytes` — altrimenti i redirect moltiplicano il budget. Il worker e' sequenziale in-process: una connessione appesa ferma i job di tutti gli Host.
- 2026-07-26 — Pinning dell'indirizzo con httpx (anti DNS rebinding): si sostituisce l'host con l'IP GIA' validato e si conserva l'identita' vera con `headers={"Host": ...}` + `extensions={"sni_hostname": ...}`, cosi' la verifica del certificato resta sul nome. Il `Location` di un redirect si risolve sull'URL LOGICO, non su quello pinnato. Inoltre: `trust_env=False` (un HTTPS_PROXY azzera la denylist facendo risolvere il nome al proxy) e `Accept-Encoding: identity` (il cap conta i byte decodificati, quindi da solo non protegge dalla bomba zlib).
- 2026-07-26 — `caplog.text` e' reso con DEFAULT_LOG_FORMAT e gli attributi passati via `extra=` NON vi finiscono mai: asserire `"segreto" not in caplog.text` su un log che usa `extra` e' tautologico — passa anche loggando in chiaro E anche cancellando il log. Asserire su `caplog.records[i].<attributo>`, e in positivo sulla forma attesa.
- 2026-07-26 — `url_redatto` deve redigere anche la QUERY, non solo lo userinfo: negli export delle OTA il link *e'* la credenziale (`.../ical/<id>.ics?s=<segreto>`). Il path si tiene, serve a identificare il feed.
- 2026-07-26 — `alembic/env.py` con l'elenco dei modelli scritto a mano e' un cancello che tace: un modulo dimenticato non fa fallire nulla, `--autogenerate` semplicemente non vede le sue tabelle e propone `op.drop_table`. I modelli si SCOPRONO (`app/registro_modelli.py`, condiviso con le guardie). Verificare con `alembic check` dopo ogni migrazione nuova. Deriva preesistente nota e non mia: indici parziali di 0001 e `regime_lettura.host_id`.
- 2026-07-26 — Guardia che filtra per convenzione di nome (`endswith("Repository")`) e' cieca a un rename: verificare che i MODULI ci siano non basta, serve un pavimento sul numero di elementi effettivamente ispezionati. Vale per ogni meta-test costruito su un pattern di nomi.
- 2026-07-26 — Dopo aver aggiunto una dipendenza npm il lockfile va rigenerato con `npm install` e VERIFICATO con `npm ci` a `node_modules` cancellato: le opzionali multipiattaforma `@emnapi/*` restano disallineate e la CI cade su tutti i job che installano il frontend. Confermato di nuovo con `@testing-library/user-event`.
- 2026-07-26 — `@testing-library/user-event` e' ora installato (serviva per submit/reset/disabled sui form). Una callback passata a `mutate(..., {onSuccess})` invocata a mano nel test va dentro `act()`, altrimenti l'aggiornamento di stato non viene flushato. `input type="url"`: jsdom applica la sanitizzazione RFC, quindi il valore letto e' gia' senza spazi ai bordi.
- 2026-07-26 — Su una superficie che mostra stato, `isError` non e' opzionale: con `data` undefined e `isPending` false il ramo «lista vuota» afferma il falso («nessun calendario collegato» su una Struttura che ne ha tre). Stessa classe di danno di NFR-2. Distinguere sempre «non ce ne sono» da «non riusciamo a leggerli».
- 2026-07-26 — LEZIONE che vale oltre questo progetto: un insieme di CHECKPOINT non e' un bound sul tempo. `httpx.Timeout` e' per-operazione e si azzera a ogni byte; la fase di TESTA della risposta non passa da `iter_bytes`, quindi nessun controllo applicativo la vede. Un byte di intestazione ogni 9s dentro `read=10s` tiene la connessione per centinaia di ore (h11 concede 100 KiB di intestazioni). Il bound si mette su CIO' CHE ATTENDE: eseguire l'attesa in un thread e ABBANDONARLA al residuo del budget.
- 2026-07-26 — CORREZIONE della voce sopra, che nella prima stesura diceva il falso: «i timeout per-operazione fanno morire in fretta il lavoro abbandonato» e' SBAGLIATO, per la stessa ragione per cui non sono un bound — si azzerano a ogni byte. `read=min(config, residuo)` e' un tetto per singola read e contro chi sgocciola non scatta mai; ai valori di default il `min()` e' perfino un no-op. Abbandonare un'attesa NON la termina: liberare il worker e rilasciare la connessione sono due problemi distinti. Servono TRE cose insieme: (1) costruire la risorsa FUORI dall'azione abbandonata, per avere un handle; (2) chiuderla sul percorso di scadenza, cosi' la read bloccata solleva; (3) thread `daemon=True` — i thread di `ThreadPoolExecutor` non lo sono e `shutdown(wait=False)` non li stacca (un hook atexit li fa `join`), quindi un abbandono blocca l'uscita del processo e un SIGTERM resta appeso fino al SIGKILL, che ammazza il job in volo.
- 2026-07-26 — Corollario: una perdita di risorse in un job durevole si ACCUMULA. Un endpoint che accoda un job per ogni POST senza tetto, piu' un poller periodico, trasformano un thread perso per fetch in una perdita per ciclo — a 15 minuti di cadenza un solo feed ostile sostiene ~1100 thread in stato stazionario. E il presidio non e' «il worker e' libero» (quello resta verde con thread e socket persi per giorni): e' contare i superstiti con `threading.enumerate()`.
- 2026-07-26 — Un controllo di sicurezza che sta su una riga ADIACENTE nel ciclo e' una riga cancellabile senza rompere nulla. Va FUSO nella funzione che produce il valore su cui opera: il divieto di declassamento `https -> http` sta dentro il calcolo del prossimo hop, quindi il prossimo hop non si calcola senza passare dal controllo. Regola generale: se una mutazione «cancella la chiamata» lascia la suite verde, il controllo e' nel posto sbagliato, non manca un test.
- 2026-07-26 — Un test che rilegge il PROPRIO file sorgente e asserisce la presenza di un letterale che sta sulla riga dell'assert non puo' fallire. Una guardia si verifica facendole ESAMINARE un caso costruito (file finto in `tmp_path`) e pretendendo che lo segnali — piu' il caso innocuo che non deve segnalare.
- 2026-07-26 — Una guardia che filtra per convenzione di nome va corretta togliendo il filtro, non aggiungendo un pavimento numerico: il pavimento va tenuto aggiornato a mano e con 12 contro 20 metodi servivano due rinomine per farlo mordere. Ispezionare OGNI classe pubblica del modulo rende la rinomina catturata per costruzione.
- 2026-07-26 — Una guardia i cui BERSAGLI si derivano dalla stessa funzione che sta testando e' autoreferenziale: aggiungere un modulo alla sua allowlist lo fa sparire dal controllo con tutti i test verdi. I bersagli si derivano dal FILESYSTEM (`app/*/models.py`, `__tablename__` letto con `ast`).
- 2026-07-26 — `Accept-Encoding: identity` e' una richiesta, non una garanzia: `iter_bytes()` decodifica in base al `Content-Encoding` della RISPOSTA. Un cap sui byte decodificati non limita la memoria — una bomba zlib da 400 KB arriva a 145 MiB — e un chunk che decodifica a vuoto non emette eventi, quindi salta anche i controlli nel ciclo. Va RIFIUTATA la risposta con codifica inattesa.
- 2026-07-26 — Pinning dell'indirizzo: `parti._replace(netloc=...)` sostituisce l'INTERO netloc e cancella lo userinfo, quindi httpx non deriva piu' il BasicAuth (un feed credenziato prende 401). E `parti.hostname` restituisce un IPv6 SENZA parentesi: nell'header `Host` RFC 9110 le vuole, e con la porta diventa impossibile da parsare. Conservare userinfo e mettere le quadre in entrambi i posti.
- 2026-07-26 — `pytest-timeout` va configurato GLOBALMENTE (`timeout = N` in `[tool.pytest.ini_options]`), non solo con un marker: e con `--strict-markers`, altrimenti se il pacchetto esce dal gruppo dev il marker degrada a warning e la protezione svanisce in silenzio.
- 2026-07-26 — Quando un test asserisce «quante volte e' successo X» al posto di «com'e' finito», e' fragile: nel test sul budget dei redirect il terzo hop poteva legittimamente partire (0,25+0,25 < 0,6) e l'assert `<= 2` cadeva a caso sotto carico. La proprieta' da asserire era l'ESITO — con budget per hop il fetch riesce, con budget totale no.
- 2026-07-26 — Un handler di test che scrive su una socket abbandonata dal client fa stampare a `socketserver` un traceback che sembra un errore del test. Avvolgere la risposta in `try/except OSError` e chiudere: nei test di scadenza l'abbandono e' l'esito ATTESO.
- 2026-07-26 — Spostare un dato di fixture nel posto giusto puo' rendere ROSSO un test che passava, e allora il test aveva un'asserzione falsa: le proprieta' iCal sconosciute il parser le RACCOGLIE (e' il normalizzatore a ignorarle), e l'assert «non compaiono» passava solo perche' la proprieta' stava a livello VCALENDAR, fuori dai VEVENT ispezionati.
- 2026-07-26 — Un test che asserisce una proprieta' su «i superstiti immediati» e' condizionato a una corsa: se la chiusura e' veloce la lista e' vuota e il ciclo `for` non esegue nessuna asserzione. Per pinnare una proprieta' di un thread abbandonato serve un'azione che BLOCCA su un `Event` controllato dal test, cosi' il thread e' vivo con certezza nell'istante dell'ispezione.
- 2026-07-26 — Una guardia che confronta lo scan del filesystem con `Base.metadata` del processo CORRENTE e' sconfitta dagli import di collection: pytest ha gia' importato i moduli, quindi le tabelle ci sono anche se la discovery le salta — verde in suite completa, rosso solo lanciando il file da solo. La forma peggiore. La scoperta va fatta girare in un interprete FRESCO (`subprocess` + `sys.executable`) e si confronta cio' che riporta QUEL processo.
- 2026-07-26 — Una sentinella che esercita solo l'helper non prova il VISITATORE: cancellare il ramo `ast.Import` non faceva cadere nulla perche' nessun sorgente veniva mai attraversato. La funzione da testare deve prendere il SORGENTE (non il percorso), cosi' la si parametrizza su codice finto — la stessa forma applicata alla guardia delle migrazioni.
- 2026-07-26 — Un test che vuole dimostrare l'assenza di proxy deve azzerare anche `NO_PROXY`/`no_proxy`: con `no_proxy=*` (comune nelle immagini CI) httpx ripristina il transport diretto per il loopback e il mutante senza `trust_env=False` sopravvive.
- 2026-07-26 — Ogni guardia che ispeziona una cartella vuole un pavimento sul numero di bersagli trovati: puntata su un percorso rinominato passerebbe ispezionando zero file. Vale per migrazioni, repository, modelli — e' la stessa classe delle allowlist che muoiono in silenzio.
- 2026-07-26 — `input[type="url"]`: la sanitizzazione sta nel SETTER di `value` di jsdom (e dei browser reali), non nel layer di battitura, quindi sia `userEvent.type` sia `fireEvent.change` la subiscono e una `.trim()` su spazi ASCII e' codice morto anche in produzione. Cio' che il sanitizer NON tocca e' lo spazio insecabile U+00A0 — che e' esattamente quello che arriva incollando da una pagina web e che `String.trim()` rimuove: e' con quello che si pinna la `.trim()`, ed e' la ragione per cui la `.trim()` serve.
- 2026-07-26 — Uno stub «finto» non si puo' passare a `ipaddress.ip_network.__contains__`: solleva `TypeError` e il test misura quello. Per simulare una classificazione futura si SOTTOCLASSA un indirizzo vero sovrascrivendo la property (es. `is_global`).
- 2026-07-26 — Una clausola di sicurezza puo' essere legittima come ASSICURAZIONE IN AVANTI anche senza copertura odierna (es. `not is_global`: nessuna classe in cui sia il solo motivo di rifiuto). Quello che non e' legittimo e' un commento o un test che AFFERMANO una copertura che non c'e': si tiene la riga, si dichiara il perche' vero, e si pinna per come e' osservabile.
- 2026-07-26 — **Un `304` si accetta SOLO come risposta a una richiesta condizionale.** Un 304 non sollecitato non afferma «nulla e' cambiato»: non c'e' niente che il portale possa aver confrontato. Accettarlo come run riuscito congela il feed PER SEMPRE, con esito riuscito e quindi in silenzio. La decisione di mandare i validatori e quella di leggere il 304 vanno tenute nella STESSA funzione, altrimenti si separano e la seconda sopravvive alla prima.
- 2026-07-26 — Corollario: i validatori di cache (`ETag`/`Last-Modified`) si memorizzano DOPO la riconciliazione riuscita, mai dopo un run fallito — un validatore scritto su un corpo non importato fa rispondere 304 a dati che nel database non sono mai arrivati. E si SOSTITUISCONO, non si fondono: una 200 senza `ETag` deve azzerare quello vecchio.
- 2026-07-26 — `Last-Modified` si rimanda VERBATIM. Riformattarlo fa fallire il confronto SEMPRE, il 304 non arriva mai, e il sintomo e' l'assenza di un risparmio — cioe' nessun sintomo. I difetti che si manifestano solo come «non succede la cosa buona» non si scoprono da soli: vanno pinnati con un test sulle intestazioni realmente inviate.
- 2026-07-26 — **Un contatore di fallimenti si DERIVA dalla traccia, non si mantiene.** Un contatore mantenuto ha due punti di scrittura, l'incremento e l'azzeramento al primo successo, e il secondo e' quello che si dimentica: l'alert suona per sempre su un servizio guarito, e un alert che suona sempre e' un alert spento. Derivandolo, l'azzeramento non e' codice da ricordarsi. Stessa regola gia' applicata a `ultimo_sync_riuscito_il`.
- 2026-07-26 — L'alert su soglia si emette all'ATTRAVERSAMENTO (`consecutivi == soglia`), non a ogni fallimento oltre soglia: un portale giu' per un giorno con tick da 15' produrrebbe 96 righe identiche.
- 2026-07-26 — **Un test di gara su `FOR UPDATE SKIP LOCKED` che conta solo gli esiti non vede niente.** Senza `SKIP LOCKED` il conteggio resta comunque 1, perche' in READ COMMITTED chi aspetta poi rivaluta il `WHERE` e scarta la riga gia' presa. La proprieta' vera e' «gli altri NON bloccano», e si osserva solo tenendo le transazioni APERTE finche' tutti gli N contendenti hanno tentato — con una seconda barriera fra i client. Rosso osservato: `BrokenBarrierError`, non un assert.
- 2026-07-26 — Namespace degli advisory lock (RT-3 chiusa): vivono tutti in `app/core/lock.py`, mai in linea nel modulo che li usa, e una guardia AST vieta `pg_advisory_*` altrove. Due percorsi con lo stesso namespace si serializzano a vicenda SENZA alcun errore: il sintomo e' latenza sotto carico, quindi si scopre in produzione e si attribuisce al database. `1001` = cap Strutture, `1002` = bootstrap sync periodico.
- 2026-07-26 — Il lock di un bootstrap periodico va preso PER RISORSA (per feed), non su una chiave costante: un lock globale darebbe la stessa post-condizione nel test di gara e passerebbe, poi con centinaia di risorse il bootstrap diventerebbe una fila indiana e nessun test lo direbbe. Serve un test dedicato che dimostri la granularita'.
- 2026-07-26 — Un job periodico nasce SEMPRE con `due_at` nel futuro: gia' scaduto verrebbe preso nello stesso giro di worker che l'ha creato e il poller girerebbe in ciclo stretto. E' anche la proprieta' su cui poggia la priorita' dei job on-demand.
- 2026-07-26 — Un handler periodico si riprogramma anche quando il LAVORO fallisce (l'unica uscita senza riprogrammazione e' la risorsa scomparsa): altrimenti il primo guasto temporaneo diventa permanente e non lo scopre nessuno. Regge perche' il service REGISTRA gli errori di rete invece di sollevarli.
- 2026-07-26 — **TRAPPOLA: un test del poller puo' essere verde per la ragione sbagliata.** L'handler usa `client_di_produzione()`, che legge la politica di uscita di rete dalla CONFIGURAZIONE: senza il loopback ammesso ogni fetch verso il server di test viene rifiutato a monte, e il rifiuto produce `url_non_raggiungibile` — proprio uno degli esiti che il test voleva osservare. Fixture `autouse` che ammette il loopback, con il perche' scritto.
- 2026-07-26 — I parametri di configurazione temporali si VALIDANO alla costruzione: un intervallo `0` letto dall'ambiente riaccoda il job gia' scaduto e consuma la coda di tutti i tenant. Un parametro sbagliato deve fermare l'avvio, non degradare in un difetto di regime che nessuno collega alla causa.
- 2026-07-26 — Un intervallo «vicino a una data di calendario» confronta ISTANTI, non date: si converte la data locale nell'istante d'inizio di quel giorno nel fuso. Sottrarre date perde l'ora, e attraverso il cambio d'ora perde proprio i sessanta minuti che la regola esiste per proteggere.
- 2026-07-26 — `alembic check` puo' essere rosso per deriva dei MODELLI, non dello schema: un indice parziale creato in migrazione e non dichiarato sul modello fa proporre un `remove_index` — cioe' di cancellare l'indice che serve. Si chiude dichiarandolo (`__table_args__` con `postgresql_where`), senza toccare il database. E `mapped_column(unique=True, index=True)` rende un indice UNICO, non un constraint piu' un indice: se lo schema ha entrambi, e' deriva.
- 2026-07-26 — Il passo di CI che verifica lo schema fa `alembic upgrade head` da se', senza affidarsi allo stato lasciato da `pytest`: un passo che dipende dall'effetto collaterale del precedente funziona finche' qualcuno non li riordina, e allora confronta i modelli con uno schema arbitrario.
- 2026-07-26 — In GitHub Actions nessun dato scritto da chi apre la PR (titolo, corpo, nome del ramo) va interpolato con `${{ }}` dentro `run:`: l'interpolazione avviene PRIMA che la shell veda lo script, quindi un `$(...)` nel corpo diventa codice eseguito dal runner. Passa da `env:`. E un job che non ha bisogno del codice non fa checkout.
- 2026-07-26 — In uno script di CI usare `sed` POSIX e non `grep -P`: il PCRE di grep si rifiuta di girare in un locale non UTF-8 e il cancello diventa rosso per la ragione sbagliata. I corpi delle PR di GitHub arrivano con CRLF: `tr -d ''` prima di misurare una stringa.
- 2026-07-26 — Una fixture pytest importata da un MODULO DI TEST diventa una ridefinizione (F811) a ogni funzione che la chiede come parametro. Le fixture si condividono dal `conftest.py`, gli helper puri da un modulo normale. E in questo repo l'import degli helper dentro `conftest.py` va fatto LOCALMENTE nella fixture: il conftest punta `HOSTPILOT_DATABASE_URL` al db di test prima di qualunque import di `app`, e un import in testa invertirebbe l'ordine.
- 2026-07-26 — Un envelope invece di una lista nuda quando la risposta deve portare un fatto sull'INSIEME (es. «dati aggiornati alle HH:MM»): con la lista nuda il consumatore deve procurarsi il fatto da una seconda chiamata e correlarlo a mano — due letture che possono divergere e nessuna che dica quale vale.
- 2026-07-26 — Nella UI un `—` al posto di un valore assente si legge COME un valore. Se non si sa, si scrive che non si sa: e' la stessa classe di danno di NFR-2, e vale anche per un ramo «impossibile per costruzione».
- 2026-07-27 — Story 2.3: `ospite` e' la prima tabella con dati personali di TERZI. Vive in `calendario` (AD-18), e' in `TABELLE_PROTETTE` di GS-6 (l'azzeramento e' sui CAMPI, una DELETE di riga sarebbe una quarta cancellazione fuori da AD-20), e la sua retention e' `HOSTPILOT_OSPITE_RETENTION_GIORNI` (90, PROVVISORIO in attesa di R-5). Decorrenza in AD-21 e in nessun altro punto: `check_out`, o l'uscita da `attiva` se precedente.
- 2026-07-27 — Per applicare «l'uscita dallo stato X se precedente» serve una colonna che NON si muova: `aggiornata_il` avanza a ogni sync anche senza cambio di stato. Aggiunta `prenotazione.cessata_il`, scritta con `COALESCE(cessata_il, adesso)` sull'upsert — riscriverla a ogni sync rimanderebbe in avanti la scadenza di un dato personale, per sempre, un sync alla volta.
- 2026-07-27 — Quando una regola deve esistere sia in Python sia in SQL (il job non puo' leggere l'intera tabella), tenerle nello STESSO file a poche righe di distanza e scrivere un test che le confronta su una tabella di casi al confine. Il costo della duplicazione si paga una volta in un test, non novanta giorni dopo su dati che non tornano. Corollario: la conversione di fuso resta in Python (un dataclass che porta gia' convertiti istante e giorno locale), cosi' il `WHERE` confronta due scalari.
- 2026-07-27 — Filtro di un job di purge/azzeramento: la condizione e' «c'e' qualcosa da azzerare?», MAI «l'ho gia' fatto una volta?». `anonimizzato_il IS NULL` sembra naturale e impedisce per sempre la scadenza di un dato REINSERITO dopo un azzeramento. Trovato per mutazione: rimuovendo quella clausola nessun test diventava rosso — era ridondante per l'idempotenza e dannosa per la correttezza.
- 2026-07-27 — Le date di calendario nel frontend restano STRINGHE ISO: `new Date("2026-10-25")` si interpreta nel fuso del browser e `getDate()` puo' dare il giorno prima. L'aritmetica si fa sul numero di giorno dall'epoca via `Date.UTC`. Guardia strutturale sul sorgente (`griglia.guardia.test.ts`): nessun accessor locale nel modulo della griglia, nessun orologio (`new Date()`/`Date.now()`) sulla superficie che mostra «dati aggiornati alle HH:MM», e i derivati mostrati devono esistere nel contratto generato.
- 2026-07-27 — Un `overflow-x-auto` senza `tabIndex={0}` e' una violazione axe **serious** (`scrollable-region-focusable`): chi naviga da tastiera vede meta' della tabella e non ha modo di arrivare all'altra. Serve `role="region"` + `tabIndex={0}` + `aria-label`. Lo vede solo l'e2e con axe, e solo sul progetto MOBILE.
- 2026-07-27 — Playwright su progetto `mobile` (`isMobile: true`): il click su un elemento in una barra `fixed` in fondo puo' fallire con «il div radice intercetta i pointer events» quando la pagina si sta ancora componendo, e un bottone in un pannello che cresce mentre carica «scappa» da sotto il cursore. Rimedi: attendere un marcatore stabile del contenuto prima di navigare, e submit da tastiera (`campo.press("Enter")`) invece del click sul bottone.
- 2026-07-27 — La freschezza di una vista che AGGREGA piu' sorgenti e' quella della piu' vecchia (`min`), e diventa «non lo so» appena una non ha mai prodotto dati. Il `max` e' aritmeticamente vero e falso come affermazione su cio' che l'utente sta guardando. Servono anche i conteggi (quante sorgenti, quante mute, quante in errore): altrimenti «nessuna sorgente», «sorgente muta» e «sorgente rotta» arrivano tutte come timestamp assente.
- 2026-07-27 — Griglia ed etichetta del timestamp stanno in UNA sola query TanStack: due cache sullo stesso derivato possono divergere, con una sola la divergenza e' impossibile invece che improbabile. Resta necessaria l'invalidazione di `["calendario"]` da OGNI mutazione che cambia il perimetro (Strutture, Feed).
- 2026-07-27 — Un selettore trasversale che deve filtrare una superficie in un'altra parte dell'albero non puo' tenere lo stato in `useState`: cambia solo se stesso. Context minimo (`ProviderSelezioneStruttura` in `app/providers.tsx`), non uno store.
- 2026-07-27 — L'ambiente e2e avvia API + frontend ma NON il worker, e la politica di uscita di rete rifiuta il loopback: nessun import di Feed puo' concludersi li'. Quando un AC dipende da un job concluso, dichiararlo non esercitabile invece di scrivere un test che passa per un altro motivo.
- 2026-07-27 (fix-batch epic2-2.3-p1) — **Un `try/finally` dentro un handler di job NON garantisce la riprogrammazione**: l'handler gira in `session.begin_nested()` (G-1) e il rollback del savepoint annulla anche cio' che il `finally` ha scritto. E se a fallire e' la query, la transazione resta abortita e l'INSERT del `finally` fallisce a sua volta. La forma che regge: savepoint INTERNO attorno al solo lavoro rischioso, errore registrato e non sollevato, riprogrammazione fuori dal try. Vale per ogni ciclo periodico che non deve potersi spegnere.
- 2026-07-27 — Un ciclo periodico che si spegne va provato dal percorso REALE (`run_due_jobs`), non chiamando l'handler a mano: e' il SAVEPOINT del kernel a fare la differenza, e un test che invoca la funzione direttamente non lo attraversa.
- 2026-07-27 — I parametri di configurazione si validano su `Settings` con `Field(gt=0)`, non solo nel codice che li consuma: altrimenti `import app.main` riesce con un valore assurdo e il difetto si manifesta a regime. Un docstring che dice «deve fermare l'avvio» non ferma nessun avvio.
- 2026-07-27 — **Il QueryClient va svuotato all'INGRESSO, non (solo) all'uscita.** Nasce nel root layout e sopravvive a `router.replace()` (navigazione client); se nessuna chiave contiene l'identita' dell'Host, quella del secondo Host nella stessa scheda e' byte-identica e TanStack la serve dalla cache entro `gcTime` — con dati personali dentro, NFR-14 regge sul server e cade sul client. Prima versione del fix: solo `useLogout`. Sbagliata a meta', perche' una sessione finisce anche per cookie scaduto, purge, riavvio del backend o un logout la cui risposta si perde, e su tutte quelle strade nessun `onSuccess` parte. La forma che regge e' `entra()` in `lib/api/hooks.ts`, attraversata da `useLogin` e `useRegistrazione`: svuota e POI scrive `["me"]` (l'ordine inverso cancella l'Host appena entrato).
- 2026-07-27 — Regola generale dietro il punto sopra: **quando i modi di USCIRE da uno stato non sono enumerabili ma le porte per ENTRARCI sono poche, il presidio si mette sull'ingresso.** Inseguire le uscite una per una lascia sempre quella a cui non hai pensato — e il test va scritto sulla strada che NON avevi chiuso, non su quella che avevi gia' in mano.
- 2026-07-27 — Uno stato condiviso che non deve sopravvivere alla sessione va montato DENTRO il route group autenticato (`app/(app)/layout.tsx`), non in `app/providers.tsx`: uscire smonta il provider. E' struttura, non «ricordarsi di azzerare al logout». Sintomo del difetto: un `<select>` che si presenta vuoto dichiarando di non filtrare, e una query bloccata su 404 stabile perche' la chiave non cambia.
- 2026-07-27 — `opacity-*` su un contenitore composita l'opacita' su TUTTO il contenuto, testo incluso: a 0.7 su `text-xs` il contrasto scende da 4.58:1 a 2.67:1, sotto la soglia AA. Per attenuare un elemento si toccano bordo e sfondo, mai il testo — soprattutto quando e' il testo a portare lo stato (UX-DR4).
- 2026-07-27 — **Una baseline axe misurata su una superficie SENZA dati audita il telaio, non il contenuto**, e da' la falsa impressione di funzionare proprio perche' e' verde. Se l'ambiente non puo' creare i dati, si intercetta la risposta dell'API con `page.route` + le intestazioni CORS: browser vero, CSS vero, contrasto vero, e finto solo il payload. Le date dello stub vanno derivate dal parametro REALMENTE richiesto, altrimenti cadono fuori dal periodo aperto e il test torna a misurare una griglia vuota.
- 2026-07-27 — **Su Windows non far mai passare un file di questo repo da `Get-Content -Raw` + `Set-Content`**: PowerShell 5.1 rilegge l'UTF-8 come ANSI e lo riscrive doppio-codificato — i sorgenti sono pieni di accenti e di `—`, quindi il danno e' garantito e silenzioso. Per una mutazione temporanea: modificare con lo strumento di edit e ripristinare con `git checkout -- <file>`, mai con un round-trip di shell.
- 2026-07-27 — **Una guardia strutturale va puntata dove il difetto puo' nascere, non dove e' gia' impossibile.** Il divieto di accessor di data locale applicato al solo modulo puro lasciava verde un `new Date(voce.check_in).getDate()` nel componente che riceve le voci dall'API — cioe' esattamente la ricomputazione vietata. E una sentinella che confronta la lista con se stessa prova che la lista e' scritta, non che sia applicata: la funzione da testare prende il SORGENTE, non il percorso.
- 2026-07-27 (Lotto A) — **`git` nella runtime dell'agente: il ref locale `main` e' VECCHIO.** `multica repo checkout` crea il branch di lavoro da `origin/main` ma non muove `main`, che qui era fermo a un commit di settimane prima: un `git checkout -b <nuovo> main` ha riportato l'intero working tree indietro (259 file -> 34) con un «working tree clean» rassicurante. Diramare SEMPRE da `origin/main` dopo `git fetch`, e verificare `git log --oneline -1` prima di scrivere codice.
- 2026-07-27 (Lotto A) — Retention del `sommario` (AD-21 esteso, MYL-47): l'azzeramento del `sommario` NON e' una colonna in piu' della `UPDATE ospite`. L'Ospite non nasce mai dal sync — l'unico `OspiteRepository.add` e' il percorso manuale dell'Host — quindi il caso PRIMARIO e' la Prenotazione scaduta con il nome nel `SUMMARY` e **nessuna riga `ospite`**: serve una seconda `UPDATE` guidata da `prenotazione`, con il proprio `sommario IS NOT NULL`, dentro lo STESSO savepoint interno (fuori riaprirebbe E2-F1 dalla porta accanto).
- 2026-07-27 (Lotto A) — **Azzerare un campo e impedirne il ripopolamento sono due presidi diversi, e vivono in punti diversi.** Il job risponde a «c'e' qualcosa da azzerare?»; il divieto di riscrittura sta nell'UPSERT (`case((Prenotazione.anonimizzato_il.is_not(None), Prenotazione.sommario), else_=excluded.sommario)`). Metterlo nel filtro del job reintrodurrebbe il P1 gia' chiuso (dato reinserito che non scade mai piu'); ometterlo dall'upsert fa tornare il campo E lascia `anonimizzato_il` ad attestare un azzeramento non piu' vero — un'evidenza che mente, peggio dell'assenza di evidenza. La guardia e' sul CAMPO: `check_in`/`check_out`/`stato` continuano a sincronizzare.
- 2026-07-27 (Lotto A) — Per far «riusare la stessa procedura» a un job periodico e a un endpoint su richiesta (NFR-15) si parametrizza la SELEZIONE, non si scrive un secondo azzeratore: `azzeramento.py` espone un dataclass `Selezione` (un predicato per tabella) con costruttori nominati, e `esegui()` non cattura eccezioni ne' fa commit — il recupero (savepoint, log, riprogrammazione) e' una scelta del chiamante, e il job e la richiesta la fanno in modo opposto. Il test che prova che sia davvero la stessa procedura e' quello che verifica che la richiesta copra anche il `sommario`.
- 2026-07-27 (Lotto A) — Un endpoint `/interno` nuovo in un modulo diverso da `config_normativa` importa `AdminToken` da `app.config_normativa.deps` (come `CurrentHost` da `identity.deps`): sono le due sole protezioni ammesse da `test_auth_convention.py`, e `/api/v1/interno/**` deve dipendere SOLO dal token, mai dalla sessione. Candidato futuro: spostare `require_admin_token` in `app/api/`, dove l'import non attraverserebbe due moduli di dominio.
- 2026-07-27 (Lotto A) — Aggiungere uno schema Pydantic in `app/calendario/schemas.py` richiede di classificarlo in `test_superfici_feed_convention.py` (GS-7), e una tabella nuova richiede `TABELLE_DA_SVUOTARE` in `conftest.py` (GS-2): sono guardie che falliscono SUBITO, ma solo se si ricorda che esistono. Checklist per ogni tabella nuova in un modulo di dominio: modello + migration + `alembic check` + lista di TRUNCATE + `host_id` NOT NULL con FK (G-3).
- 2026-07-27 (Lotto A, P1 di Murat) — **LEZIONE CHE VALE OLTRE IL PROGETTO: un predicato condiviso fra un percorso che RIPASSA e uno che si esegue UNA VOLTA SOLA e' un difetto in attesa, anche quando e' corretto per entrambi presi singolarmente.** Il job di retention filtra «c'e' qualcosa da azzerare?» e puo' permetterselo perche' compensa al giro dopo; la richiesta GDPR usava lo stesso filtro e non ha compensazione. Se il campo era gia' vuoto nell'istante della richiesta, nessuna riga veniva toccata, l'evidenza non veniva scritta, la guardia di non-ripopolamento non si armava — e il portale ripubblicava il nome di chi aveva chiesto la cancellazione. La domanda da farsi quando si fattorizza: **questo percorso ha una seconda occasione?** Se no, gli serve un passo in piu' (qui il SIGILLO), non un filtro diverso — cambiare il filtro condiviso avrebbe rotto l'altro percorso.
- 2026-07-27 (Lotto A, P1 di Murat) — Corollario sui test: la suite era verde perche' il test che costruiva ESATTAMENTE la forma difettosa (`test_l_evidenza_si_scrive_anche_quando_non_c_era_nulla_da_azzerare`) si fermava a `(0, 0)` e non guardava ne' l'evidenza ne' un sync successivo. **Un test che costruisce lo scenario giusto e asserisce troppo poco e' peggio di un test assente: occupa il posto di quello che avrebbe morso.** E il rosso su `main` per una feature nuova (import mancanti, colonna mancante) e' poco informativo: la copertura vera si misura con la mutazione — rompere un invariante alla volta e verificare QUALE test cade.
- 2026-07-27 (Lotto B) — **TRAPPOLA che vale oltre il progetto: un test di gara puo' restare verde perche' i thread non partono mai insieme.** La `threading.Barrier` allinea l'ISTANTE di partenza, non il costo di cio' che viene dopo: la prima esecuzione di una istruzione paga l'apertura della connessione e la compilazione SQLAlchemy, che bastano a scaglionare gli otto contendenti oltre la finestra critica. Misurato: con tre percorsi identici messi in gara, il PRIMO della lista — quello che paga la compilazione per tutti — restava verde anche col vincolo rimosso, gli altri due cadevano subito. Rimedio: eseguire il percorso sotto test una volta e fare `rollback` PRIMA della barriera (niente resta scritto e il lock consultivo, legato alla transazione, si rilascia). Dopo: 3 su 3 rossi in 5 giri su 5.
- 2026-07-27 (Lotto B) — Namespace advisory lock aggiornati: `1003` purge sessioni, `1004` retention Ospite, `1005` purge coda `job`. I tre sono SINGOLETTI (un solo ciclo per l'intero sistema), quindi la chiave e' costante — `blocca_singoletto` in `app/core/lock.py` — e a distinguerli resta il solo namespace. Il rosso della guardia sulla granularita' e' `BrokenBarrierError`, non un assert: con namespace condiviso gli altri thread non raggiungono mai la barriera.
- 2026-07-27 (Lotto B) — La forma `SELECT`-poi-`schedule` dei bootstrap periodici e' alla **quarta** occorrenza (sync per Feed, purge sessioni, retention Ospite, purge coda job). Non fattorizzata per decisione di processo: la de-duplicazione e' materia di Fahad. Il dato e' in MYL-57 per la retrospettiva.
- 2026-07-27 (Lotto B) — `job` ha ora una retention (`app/core/manutenzione.py`, `HOSTPILOT_JOB_RETENTION_GIORNI`). Si eliminano SOLO le righe `completed`: le `failed` restano perche' il loro `last_error` e' spesso l'unica traccia di un guasto, e cancellarle sarebbe buttare l'evidenza di un problema aperto. La finestra si misura su `created_at` e non su `due_at`, che il backoff RISCRIVE a ogni tentativo e quindi non e' la data di nulla.
- 2026-07-27 (Lotto B) — **Misurare un piano di esecuzione senza duplicare la query**: si esegue il percorso di produzione con un listener `before_cursor_execute` sull'engine, si cattura l'istruzione REALE con i suoi parametri, e si chiede `EXPLAIN (FORMAT JSON)` di quella. Un test che riscrive l'SQL a mano misura la query del test. Il rosso si ottiene nella stessa esecuzione: `DROP INDEX` dentro un `begin_nested()` e `rollback` (il DDL in Postgres e' transazionale). Numeri su 20.000 righe `completed`: idempotenza del bootstrap con `ix_job_attivi` = Index Scan, 3 buffer, 0,08 ms; senza = Seq Scan, 541 buffer, 20.000 righe scartate, 3,06 ms. Lo SCODAMENTO (`claim_due`) non era degradato: `ix_job_due` e' parziale su `status = 'pending'` ed e' esattamente il suo predicato.
- 2026-07-27 (Lotto B) — `.gitattributes` esiste ora alla radice: `* text=auto eol=lf` piu' `*.ics text eol=lf`. Serve ai fixture di formato — il loro README dichiara «i file sono in LF» e su Windows con `core.autocrlf=true` il working tree li dava in CRLF, cioe' `read_bytes()` restituiva byte diversi a seconda della macchina. L'indice era gia' tutto LF, quindi nessuna renormalizzazione di massa.
- 2026-07-27 (Lotto B) — `test_calendario_poller.py::test_l_ENTRYPOINT_del_worker_accoda_tutti_i_cicli` asserisce l'uguaglianza ESATTA dell'insieme dei cicli accodati all'avvio: ogni ciclo periodico nuovo va aggiunto li' oltre che in `app/worker.py`. Ha morso subito, come previsto dalla sua docstring — e' il modello di come si scrive una guardia contro un'assenza.
- 2026-07-27 (Lotto A) — `anonimizzato_il` fa doppio servizio ed e' deliberato: sul job attesta «campo azzerato», sul percorso su richiesta attesta «su questa riga la richiesta NFR-15 e' stata evasa» (vero anche a zero campi cancellati). La distinzione non si perde perche' i conteggi restano separati e finiscono in `azzeramento_audit`. Debito noto e dichiarato: la guardia non ha condizione di rilascio — una Prenotazione futura ripristinata dal portale resta senza `sommario` per sempre (F2, P2 in attesa di Fahad).
- 2026-07-30 (MYL-68) — `regime_lettura` e' un REGISTRO, non un flag: la revoca della conferma di lettura e' `revocata_il` (NULL = vale ora) e la riga resta. Forma scelta fra le due ammesse dalla decisione: **una riga per giro di soglia**, la riconferma dopo una revoca ne apre una nuova. Il solo `revocata_il` su riga singola NON bastava — la riconferma avrebbe sovrascritto `confermato_il`/`revocata_il` del giro precedente, cioe' avrebbe spostato in avanti la stessa perdita di evidenza che la decisione chiude. Corollario: `uq_regime_lettura_host_id` vieterebbe la storia, quindi l'invariante diventa un indice unico PARZIALE (`WHERE revocata_il IS NULL`) — «al piu' una conferma VALIDA per Host», che e' cio' che rende legittimo `one_or_none()`. Riconferma mentre la conferma vale: aggiorna la riga, non ne aggiunge una (il registro cresce di un giro di soglia, non di un click).
- 2026-07-30 (MYL-68) — **`self._db.delete(istanza)` e' invisibile alla guardia per tabella di GS-6**: il controllo AST non puo' attribuire una cancellazione di istanza a una tabella, quindi aggiungere una tabella a `TABELLE_PROTETTE` senza aggiungere il suo modulo a `MODULI_SENZA_CANCELLAZIONI` lascia la guardia verde proprio sulla forma del difetto che si vuole vietare. Ora `strutture` e' fra i moduli senza cancellazioni, e `MODELLI_PROTETTI` e' raggruppato per modulo proprietario (prima il test dei nomi importava solo `app.calendario.models`, quindi la prima tabella protetta fuori da `calendario` lo faceva cadere).
- 2026-07-30 (MYL-68) — Verificare la forza dei test per mutazione richiede il codice **gia' committato**: `git checkout -- <file>` per rimuovere la mutazione riporta il file a HEAD, e su un'implementazione non ancora committata cancella il lavoro (mi e' costato riscrivere `repository.py`). Sequenza: commit, poi muta, poi ripristina. Le tre mutazioni utili qui: filtro `revocata_il` rimosso dalla lettura (5 rossi, incluso il test di Story 1.6 sul pannello), revoca che riscrive tutte le righe (1 rosso, l'idempotenza dell'evidenza), `delete` reintrodotta (6 rossi, fra cui la guardia GS-6 sul modulo proprietario).
- 2026-07-30 (MYL-75) — **«Verde in isolamento» non significa «verde sul bersaglio del merge».** Due mie PR (#52 Story 2.4 e #53 MYL-68) hanno dichiarato ciascuna una migrazione `0013` con lo stesso `down_revision = "0012"`: ognuna vedeva solo la propria, entrambe verdi, mergiate a nove secondi di distanza. Sull'albero unito `alembic upgrade head` esce con `Multiple head revisions are present` — e cade anche l'e2e, perche' `playwright.config.ts` avvia il backend proprio con quel comando. **Conflitto semantico, non testuale: nomi di file diversi, git non segnala nulla.** Alembic per giunta non alza un errore sui `revision` duplicati, solo un `UserWarning`. Guardia in `tests/test_migrations.py::TestCatenaLineare`, senza database. Regola operativa: quando esistono due PR aperte in parallelo che toccano `alembic/versions/`, guardare il ramo dell'ALTRA prima di scegliere l'ordinale.
- 2026-08-12 (Story 2.5) — **`rowcount` NON e' affidabile su un `INSERT … SELECT`**: SQLAlchemy restituisce `-1` quando il conteggio non e' disponibile, e `-1` e' VERO in un `if`. La forma «ho inserito?» si scrive con `RETURNING` e si guarda se e' tornata una riga. Il difetto qui era invisibile allo stato persistito — il vincolo faceva il suo lavoro, nessuna riga doppia — e visibile solo a valle: un evento di dominio in piu' a ogni esecuzione, cioe' (nella 2.6) una notifica ripetuta all'Host per lo stesso fatto.
- 2026-08-12 (Story 2.5) — **Un indice UNIQUE parziale su una coppia richiede ANCHE un CHECK di ordinamento canonico** (`min_id < max_id`). Con le colonne libere di essere scambiate, `(A,B)` e `(B,A)` sono righe diverse per l'indice: il vincolo esiste e non morde, e l'invariante e' violabile senza violare la lettera dell'AC. Le due meta' vanno sempre insieme.
- 2026-08-12 (Story 2.5) — In un `ON CONFLICT DO UPDATE` il `RETURNING` di Postgres vede la riga NUOVA, mai quella vecchia: «e' cancellata» si legge, «ERA attiva e ora e' cancellata» no. Per riconoscere la TRANSIZIONE serve un valore che la porti con se': qui `cessata_il` scritto con `COALESCE(cessata_il, adesso)` torna uguale ad `adesso` solo alla prima uscita. Emettere sullo stato invece che sulla transizione non da' errore: ripete l'evento a ogni sync.
- 2026-08-12 (Story 2.5) — Una `UPDATE` di massa che deve produrre **un evento per riga** ha bisogno di `RETURNING`, non del `rowcount`: sapere QUANTE righe ha toccato non permette di dire QUALI. Vale ogni volta che una transizione di stato in blocco diventa un fatto di dominio (qui `marca_rimosse_dal_feed`).
- 2026-08-12 (Story 2.5) — **Una guardia AST che elenca le forme di SCRITTURA che sa immaginare e' gia' incompleta.** La mia cercava `stato=…` e le assegnazioni, e non vedeva `literal(StatoConflitto.X, tipo)` — la forma posizionale, cioe' quella che il repository usa davvero. Si scrive al contrario, default-deny: ogni uso e' una scrittura TRANNE quelli dentro un confronto. Leggere e' il caso enumerabile, scrivere no. L'ha trovata il test di completezza della guardia stessa (pretende di trovare scritti tutti gli stati ammessi): senza quello sarebbe rimasta verde.
- 2026-08-12 (Story 2.5) — Nel far confluire due transizioni diverse in un'unica lista di «fatti da emettere», il contatore append-only che ne contava UNA cambia significato in silenzio (`sync_run.prenotazioni_rimosse_dal_feed` avrebbe incluso le annullate dal portale). I due numeri coincidono in quasi ogni scenario, quindi nessun test lo vede. Regola: un contatore persistito conta cio' che il suo nome dice; l'evento racconta l'insieme piu' largo.
- 2026-08-12 (Story 2.5) — Il timestamp mostrato accanto a un dato che resta aperto per giorni (le due colonne della Finestra di riconciliazione) si DERIVA alla lettura, non si copia sulla riga alla creazione: una copia e' una fotografia che invecchia mentre l'utente la guarda per decidere. Stessa scelta gia' fatta per `ultimo_sync_riuscito_il`. Corollario §4.2-6: quando due colonne affiancate portano orari che significano cose diverse (sync vs inserimento a mano), il server espone un FLAG (`sincronizzata`) e il client scrive l'etichetta — la falsa simmetria e' peggio dell'asimmetria.
- 2026-08-12 (Story 2.5) — Primo sottoscrittore di `outbox` del progetto: la registrazione (`subscribers.subscribe` importato da `app/worker.py`) e' un'ASSENZA che tace — se sparisce, gli eventi continuano a essere scritti e «consegnati» a zero handler, e nessun test funzionale cade. Va pinnata guardando il registro di PRODUZIONE dopo l'import dell'entrypoint, non un `EventSubscribers` costruito nel test.
- 2026-08-12 (Story 2.5, F1 di Murat) — **Idempotenza e staleness sono due proprieta' diverse, e si difendono in due punti diversi della stessa `WHERE`.** Un handler di evento puo' essere perfettamente idempotente e sbagliare lo stesso: la consegna e' asincrona, e fra la SCRITTURA del fatto e il suo CONSUMO lo stato puo' essere tornato indietro. Qui una Prenotazione `cancellata` che il portale RITIRA torna `attiva` (la clausola anti-ritorno nell'upsert copre solo `rimossa_dal_feed`), e l'evento in ritardo spegneva un Conflitto fra due prenotazioni ancora vive. La domanda da farsi su ogni handler: «il fatto che sto consumando e' ancora vero ADESSO?» — e la risposta va messa dentro l'istruzione, non prima. Riprodotto con: portale annulla (evento scritto, non consegnato) -> portale ritira -> consegna.
- 2026-08-12 (Story 2.5, F3 di Murat) — **Con chiavi primarie ordinate nel tempo (`uuidv7`) l'entita' creata per prima e' SEMPRE il `min` di una coppia canonicalizzata.** Se tutti i test fanno uscire di scena la prima entita' creata, la meta' del predicato che guarda il `max` non e' esercitata da nessuno e la mutazione su quella colonna sopravvive a suite intere. Non si vede leggendo i test: si vede solo mutando. Quando un allestimento dipende dall'ordine di creazione, l'asserzione sul lato va scritta esplicitamente (`assert conflitto.prenotazione_max_id == seconda.id`), altrimenti un cambio di generazione delle chiavi riporta il test a esercitare sempre lo stesso ramo. (Murat la propone anche alla libreria di squadra: vale oltre questo progetto.)
- 2026-08-12 (Story 2.5, F2 di Murat) — **Un helper di allestimento condiviso decide quale percorso di produzione viene esercitato, e lo decide una volta per tutti i test che lo usano.** Il mio `_con_conflitto_da_feed` sincronizzava il feed PRIMA di creare la manuale: al termine dell'import la sovrapposizione non esisteva ancora, quindi il Conflitto lo apriva sempre il percorso manuale e la rilevazione dentro `esegui_sync` si poteva cancellare con 142 test verdi. Quando due percorsi diversi portano allo stesso stato finale, l'ORDINE dell'allestimento e' il test: serve un caso per ciascun innesco, non un caso che li attraversa entrambi.
- 2026-08-12 (MYL-88, F4 di Murat) — **Uno STATO e la MEMORIA di uno stato sono due dati diversi, e tenerli sulla stessa colonna perde il secondo a ogni transizione.** Il Conflitto `gestito` che decade smette di essere `gestito`, e una guardia che leggeva `stato = 'gestito'` da quel momento non protegge piu' niente: basta che la Prenotazione torni `attiva` perche' la coppia si riapra come se l'Host non avesse mai deciso. La domanda da farsi su ogni guardia scritta sullo stato corrente: «questo stato puo' cambiare per una ragione che NON annulla la decisione che sto proteggendo?». Se si', la guardia va su una colonna che registra il fatto (`gestito_il`), non sullo stato. Corollario sui CHECK: `decaduto_il` vuole l'equivalenza (`(stato='decaduto') = (decaduto_il IS NOT NULL)`), `gestito_il` vuole l'IMPLICAZIONE — copiare la riga sopra per simmetria vieta proprio il caso «decaduto ed era stato gestito» che la colonna esiste per rappresentare.
- 2026-08-12 (MYL-88, F6) — **Un ramo di produzione che nessun percorso applicativo raggiunge si esercita dall'ALLESTIMENTO, non allargando la produzione.** La guardia anti-riapertura non aveva test perche' scrivere `gestito` da `app/` avrebbe violato l'invariante di assenza dell'AC 8. La via d'uscita non e' rilassare l'invariante: e' che a scrivere quello stato sia un helper di test che fa oggi cio' che il service della Story futura fara' domani. La guardia AST ispeziona `app/`, quindi resta imposta; i test che poggiano sull'helper non cambieranno quando il service arrivera'. Vale ogni volta che un invariante di assenza rende non testabile il codice che l'invariante protegge.
- 2026-08-12 (MYL-88) — **Il momento giusto per una colonna che nessuno legge ancora e' PRIMA della Story che la riempie, e il criterio e' l'asimmetria del costo nel tempo.** Aggiungerla oggi e' additiva e senza backfill, perche' nessuna riga ha ancora qualcosa da ricordare; aggiungerla dopo significa ricostruire fatti che nessuna tabella conserva piu' — non una migrazione piu' lunga, un dato che non esiste. Regola pratica per la review: quando un difetto e' «latente oggi, certo domani», si stima il costo del rimedio ADESSO contro il costo DOPO, non contro zero.
