# Memorie — Amelia — Senior Software Engineer (Fase 4 Implementation)

_Fatti durevoli e decisioni apprese durante il progetto HostPilot. Un fatto per voce, con data. Aggiornare via PR. Non duplicare `docs/project-context.md`._

<!-- Esempio:
- 2026-07-24 — <fatto appreso e perché conta>.
-->

- 2026-07-24 — Story 1.1 consegnata: monorepo `backend/`+`frontend/`, kernel `core`, CI. Il backend usa **uv** (non pip come il template): `uv sync --group dev`, `uv run pytest`. Test DB su PostgreSQL 18 reale via `HOSTPILOT_TEST_DATABASE_URL` (locale: Docker porta 54329); in CI `HOSTPILOT_TEST_DB_REQUIRED=1` impedisce skip silenziosi.
- 2026-07-24 — Contratto API: `backend/openapi.json` e `frontend/lib/api/schema.d.ts` sono **committati** e la CI (`api-contract`) fallisce se divergono dal codice. Dopo ogni modifica agli endpoint: `uv run python scripts/export_openapi.py` + `npm run generate:api`.
- 2026-07-24 — Catalogo `core/events.py`: registrare i tipi evento/job nella Story che li emette per la prima volta (catalogo di produzione parte vuoto; i test usano istanze `Catalog()` locali).
- 2026-07-24 — `eslint-config-next` 16 è flat-native: si importa da `eslint-config-next/core-web-vitals` e `/typescript`, senza FlatCompat (il template Next 15 va adattato).
- 2026-07-24 — shadcn/ui non ancora inizializzato (previsto con la Story 1.3, prima UI reale); e2e Playwright presente ma fuori dalla CI PR fino alla 1.3.
- 2026-07-24 — Story 1.2: FastAPI 0.139 include i router in modo **lazy** (`_IncludedRouter`): per ispezionare le route usare `route.effective_route_contexts()` (vedi `tests/test_auth_convention.py`). Il TestClient va creato con `base_url="https://testserver"` o il cookie Secure non viene mai rinviato.
- 2026-07-24 — Auth: ogni endpoint nuovo NON pubblico deve dichiarare `CurrentHost` (da `app.identity.deps`) — la guardia `test_auth_convention.py` fallisce altrimenti e va aggiornata la allowlist SOLO per endpoint davvero pubblici. `host_id` sempre dalla sessione.
- 2026-07-25 — Regola FIX-FORWARD attivata da Fahad: preparare un fix-batch è autonomo (branch `fix/<epic>-<batch>`, PR separata, verdetto di Murat), accettare (merge) è umano. Scope dei batch RIGIDO: solo i finding elencati.
- 2026-07-25 — Pattern per handler outbox/job: l'invocazione è dentro `session.begin_nested()` (SAVEPOINT per item, G-1) — un handler che scrive e poi solleva non lascia scritture parziali. I nuovi handler restano idempotenti E le loro scritture devono passare dalla sessione ricevuta, mai da sessioni proprie.
- 2026-07-25 — Debiti registrati da Murat per story future: G-3 (guardia strutturale tenant-scoping `host_id`) va consegnata CON la Story 1.4; G-5 (purge sessioni scadute come job + rate-limit login) resta proposta P2.
- 2026-07-25 — Story 1.3: `get_settings()` è lru_cache-ata e `app.main` la chiama a import-time (CORS) — nei test l'URL del DB va impostato PRIMA di ogni import di `app.*`: il conftest lo fa a modulo-import + `cache_clear()` nella fixture. Non rimuovere quelle righe.
- 2026-07-25 — Frontend: vitest senza `globals: true` → l'auto-cleanup di Testing Library non si attiva; è agganciato in `vitest.setup.ts` (afterEach(cleanup)). CLDR it-IT: separatore migliaia solo da 5 cifre ("1234,56 €"). shadcn/ui ancora NON inizializzato: attivarlo alla prima story con form complessi (1.4), è il seed ratificato G3-1.
- 2026-07-25 — Regola di gate aggiornata da Murat: dalla Story 1.3 il suo verdetto PRECEDE il merge umano — consegnare sempre con menzione a lui e non aspettarsi merge prima del verdetto.
- 2026-07-25 — Story 1.4: G-3 chiuso con `tests/test_tenancy_convention.py` — ogni NUOVO modulo di dominio è scoperto automaticamente: i suoi repository DEVONO avere `host_id` in ogni metodo pubblico e le sue tabelle `host_id` NOT NULL+FK (allowlist solo outbox/job/host). C1 chiuso: job CI `e2e` avvia backend reale via webServer Playwright (migrazioni+uvicorn, DB `hostpilot_e2e`, cookie secure=false SOLO lì) + axe serious/critical=0.
- 2026-07-25 — Alembic: `op.create_table` con colonna Enum CREA da sé il tipo Postgres — mai chiamare anche `enum.create(bind)` (DuplicateObject). Per `op.add_column` invece serve la create esplicita (vedi migrazione 0003 vs 0004).
- 2026-07-25 — Story 1.5: le tabelle di RIFERIMENTO condivise (anagrafica ISTAT, `config_normativa`) sono in allowlist esplicita in `test_tenancy_convention.py`, con un test che verifica che non acquisiscano mai un legame con `host`. `test_auth_convention.py` accetta ora due protettori: `CurrentHost` (sessione) o `require_admin_token` (endpoint `/interno`). Un modulo nuovo di dominio resta comunque sorvegliato: non aggiungerlo alle allowlist senza motivazione scritta.
- 2026-07-25 — G-5 chiuso. Worker: l'entrypoint è ora `python -m app.worker` (livello applicativo) — importa i moduli che registrano handler/subscriber e fa il bootstrap dei job periodici, poi cede a `app.core.worker` che resta il ciclo generico. `core` non deve MAI importare moduli di dominio (AD-1): ogni nuovo handler si registra aggiungendo l'import in `app/worker.py`.
- 2026-07-25 — Periodicità durevole senza scheduler esterno: l'handler si RIPROGRAMMA alla fine di ogni esecuzione (`_riprogramma`) e un bootstrap idempotente (`assicura_purge_periodico`) rimette in coda il ciclo se manca — così un restart non lo perde e non si creano duplicati.
- 2026-07-25 — Rate-limit login: tabella `tentativo_login` SENZA `host_id` (si scrive prima di sapere se l'account esiste: legarla all'Host rivelerebbe quali email sono registrate). È in allowlist `TABELLE_PRE_AUTENTICAZIONE` di `test_tenancy_convention.py`, sorvegliata dal test che vieta legami con `host`. Il freno si applica PRIMA di verificare le credenziali e vale anche per email inesistenti, altrimenti la differenza di comportamento diventa enumerazione.
- 2026-07-25 — F-1 chiuso: il cap Strutture è serializzato per Host con `pg_advisory_xact_lock(namespace, hashtext(host_id))` preso PRIMA del conteggio (`StrutturaRepository.blocca_creazioni_dell_host`). Lock consultivo legato alla transazione: si rilascia da solo a commit/rollback e non tocca tabelle di altri moduli (AD-18). Namespace 1001 riservato a questo uso: se servirà un altro advisory lock, usare un namespace diverso.
- 2026-07-25 — Riprodurre un TOCTOU nei test: 2 thread non bastano (l'interleaving spesso non capita), 8 thread con `threading.Barrier` lo colgono in modo affidabile. Evitare di strumentare la finestra critica con un barrier interno: con un fix basato su lock quel barrier va in deadlock.
- 2026-07-25 — F-3 chiuso: `secrets.compare_digest` va chiamato sui BYTE (`.encode()`), mai su stringhe che possono contenere non-ASCII — le intestazioni HTTP sono decodificate latin-1 e il confronto su str solleverebbe TypeError (500 al posto di 403).
- 2026-07-25 — Story 1.6: il Regime fiscale è una FUNZIONE del conteggio Strutture, non uno stato (AD-12) — un test vieta colonne `regime*`/`fiscal*` sulle entità. L'unico stato persistito è la conferma di lettura del pannello (`regime_lettura`), e il rientro sotto soglia la azzera: senza quello, dopo ridiscesa+risalita il pannello non ricompariva (UJ-4 edge).
- 2026-07-25 — Frontend: quando un valore derivato ha una sua query (es. `regime-fiscale` che dipende dal conteggio Strutture), OGNI mutazione sulla sorgente deve invalidare anche quella cache — centralizzato in `invalidaStruttureERegime`. Bug trovato solo dall'e2e: i test di componente con hook mockati non lo vedono.
- 2026-07-25 — Playwright: `window.confirm` è rifiutato di default; accettarlo con `page.once("dialog", d => d.accept())` prima del click, altrimenti il test fallisce a valle in modo fuorviante.
- 2026-07-25 — Validità temporale (config_normativa): una nuova riga chiude le aperte con `valido_dal <= nuovo` (pari decorrenza inclusa: delibera ri-emessa) e la risoluzione ordina per `valido_dal DESC, creato_il DESC`. Regola generale per ogni futura tabella a validità temporale: chiusura + tiebreaker deterministico, mai affidarsi all'ordine di ritorno del DB.
- 2026-07-25 — Anagrafica: NON committare codici ISTAT dei Comuni inventati. Le 20 Regioni (codici stabili) sono seedate nella migrazione 0005; i Comuni si importano dal CSV ufficiale con `python -m app.config_normativa.importa_comuni <file.csv>` (upsert idempotente). Nei test si usano codici sintetici marcati come tali.
- 2026-07-25 — Frontend: un `<datalist>` dentro la `<label>` finisce nel testo accessibile del campo e rompe `getByLabelText` (oltre all'a11y): tenerlo fuori e usare `htmlFor`/`id` espliciti.
- 2026-07-25 — Il repo ha **SonarCloud** come check obbligatorio sulle PR (quality gate su New Code) oltre a GitHub Actions: dopo ogni push controllare ENTRAMBI. Regole che ci hanno bloccato: action di terze parti da pinnare al commit SHA (S7637), `uv sync`/`npm ci` da eseguire senza esecuzione di script (`--locked --no-build --no-install-project`, `--ignore-scripts`), mai `npx` in CI (usare `npm exec --no --`), `response_model` FastAPI ridondante col return type (S8409), `role="status"` → `<output>` (S6819), props React `Readonly` (S6759).
- 2026-07-25 — In CI il progetto backend NON è installato nel venv (`--no-install-project`): i comandi girano con cwd=backend e `uv run --no-sync`. Uno script in `scripts/` deve aggiungersi la dir backend a `sys.path` (python mette in path la dir dello script, non la cwd) — vedi `scripts/export_openapi.py`.
- 2026-07-25 — Dopo aver aggiunto una dipendenza npm, rigenerare e committare `package-lock.json` verificandolo con `npm ci` (non basta `npm install`): le opzionali multipiattaforma `@emnapi/*` possono restare disallineate e far fallire tutti i job che installano il frontend.
- 2026-07-25 — Playwright: vietato importare da un file `*.spec.ts` a un altro — gli helper condivisi vanno in file non-spec (es. `e2e/axe-utils.ts`). La nuova regola eslint react-hooks vieta setState sincrono negli effect: per inizializzare form da dati async usare componente figlio con `key` + useState(prop).
- 2026-07-26 — Story 2.1 (primo codice di rete in USCITA del progetto). Il client HTTP verso i Feed si INIETTA come parametro del service (`esegui_sync(..., client=...)`), mai come import globale: è la condizione perché i test sostituiscano il confine senza mockare il service. In test la rete si stub-a AL TRASPORTO con `tests/server_feed.py` (ThreadingHTTPServer su 127.0.0.1 che sa fare redirect, Content-Length bugiardo, chiusura anticipata, silenzio).
- 2026-07-26 — GS-1 attiva: `tests/conftest.py::isolamento_di_rete` è `autouse` e blocca connect/create_connection/getaddrinfo fuori dal loopback. La sua eccezione deriva da **BaseException** di proposito — `valida_destinazione` converte `OSError` in «destinazione non ammessa» e il client converte gli errori di rete in «URL non raggiungibile»: con `Exception` la guardia verrebbe assorbita e un test senza risolutore iniettato passerebbe per il motivo sbagliato.
- 2026-07-26 — `Base.metadata.tables` contiene SOLO le tabelle dei moduli importati: una guardia che li legge prima non fallisce, tace. `test_tenancy_convention.py` era cieco alle tabelle di un modulo non raggiungibile da `app.main`. Da qui ogni guardia sul modello passa da `tests/modello.py::carica_modelli()` (importa anche `app.core.jobs`/`outbox`, che dichiarano tabelle fuori dai moduli di dominio).
- 2026-07-26 — `get_settings()` è lru_cache-ata: un test che cambia un env var lascia il valore in cache anche dopo il ripristino di `monkeypatch`, e il test dopo vede un'impostazione che non ha chiesto. Fixture `autouse` `configurazione_pulita` che svuota la cache DOPO ogni test (non basta farlo dentro il test).
- 2026-07-26 — Idempotenza dell'import: `INSERT ... ON CONFLICT (feed_id, ical_uid) DO UPDATE` in UNA istruzione, zero pre-check applicativi. `RETURNING (xmax = 0)` distingue inserita da aggiornata — senza, il conteggio «importate» è una stima. Rosso verificato: con check-then-write e senza il UNIQUE, 8 thread producono 8 righe duplicate.
- 2026-07-26 — «Job prioritario» si ottiene con `due_at = utcnow()` (già scaduto), NON aggiungendo una colonna `priority` a `job`: i cicli periodici hanno sempre `due_at` nel futuro, quindi l'on-demand li precede senza toccare `core` per il bisogno di un solo dominio.
- 2026-07-26 — Un handler di job non fa MAI `commit`: gira dentro `session.begin_nested()` (SAVEPOINT per item, G-1) e un commit interno lo scavalcherebbe. Corollario in `calendario`: `esegui_sync` non solleva sugli errori di rete/feed — li REGISTRA come `sync_run` fallito, altrimenti il rollback del savepoint cancellerebbe la traccia insieme all'eccezione.
- 2026-07-26 — Invariante di dato di AD-4/AD-19 imparato a caro prezzo (E2-G3, R2-C): `rimossa_dal_feed` si applica SOLO dopo un parse completo e validato (`END:VCALENDAR` presente, nessun VEVENT non chiuso), e `uid_presenti` deve includere anche gli uid degli eventi MALFORMATI — un VEVENT con UID ma senza DTEND è nel feed, escluderlo marcherebbe rimossa una Prenotazione viva. Con l'implementazione ingenua una risposta troncata marca `rimossa_dal_feed` l'intero calendario.
- 2026-07-26 — Politica di uscita di rete (NFR-17): denylist sull'indirizzo RISOLTO, tutti gli indirizzi restituiti dal DNS devono passare (un round-robin con un indirizzo interno sarebbe un bypass), redirect NON seguiti dal client HTTP ma un hop alla volta con rivalidazione, cap di dimensione applicato in streaming E sul `Content-Length` dichiarato. Oltre a loopback/privati/link-local servono CGNAT `100.64/10`, `192.0.0.0/24`, `100.100.100.200` (metadati Alibaba), NAT64 `64:ff9b::/96` e lo srotolamento degli IPv4-mapped. Il rifiuto usa la STESSA categoria d'errore di «irraggiungibile»: distinguerle è un canale per mappare la rete interna. Limite noto non chiuso: DNS rebinding (servirebbe il pinning dell'indirizzo nel trasporto).
- 2026-07-26 — `sa.Enum` condiviso da due tabelle nella stessa migrazione: il PRIMO uso dichiara i valori (e crea il tipo), i successivi usano `sa.Enum(name="...", create_type=False)` — idioma già in migrazione 0005. Nei modelli, una sola istanza `Enum` riusata dalle due colonne.
- 2026-07-26 — A2 chiusa: `.github/workflows/ci.yml` ha ora `permissions: contents: read` a livello di workflow. Un job che dovrà scrivere dichiarerà il permesso al proprio livello.
- 2026-07-26 — TRAPPOLA SQLAlchemy che costa un P0: `colonna.not_in([])` su una lista VUOTA non e' «nessuna riga», e' `(col NOT IN (NULL) OR (1 = 1))` — vero per OGNI riga. Una UPDATE con quel filtro degenera in «tutte». Ogni `in_`/`not_in` costruito da una lista calcolata a runtime vuole una guardia esplicita sul caso vuoto, nel punto piu' basso possibile (il repository), oltre alla decisione a monte.
- 2026-07-26 — Corollario del P0: una guardia «nessun evento» non copre «nessun evento IDENTIFICABILE». Nel sync iCal si decide su `uid_presenti`, non su `analizzato.eventi`: un VCALENDAR chiuso i cui VEVENT non hanno UID passa il parser, ha eventi, e arriva alla riconciliazione con l'insieme di confronto vuoto.
- 2026-07-26 — Un handler di job gira dentro `session.begin_nested()`: QUALUNQUE eccezione non catturata annulla anche la riga di traccia che l'handler ha appena scritto. Su un loop che elabora contenuto di terze parti la cattura va larga — ma SOLO attorno alla parte PURA (il normalizzatore): allargarla attorno a una scrittura DB farebbe proseguire su una transazione abortita. I limiti di lunghezza delle colonne si applicano a monte, nel codice puro, senza lasciare sbagliare il DB.
- 2026-07-26 — `httpx.Timeout` e' solo per-operazione: un portale che sgocciola un byte appena dentro il timeout di lettura non fa scattare nulla e puo' tenere la connessione per mesi. Serve una deadline monotona calcolata UNA volta per fetch, controllata per hop e dentro il ciclo di `iter_bytes` — altrimenti i redirect moltiplicano il budget. Il worker e' sequenziale in-process: una connessione appesa ferma i job di tutti gli Host.
- 2026-07-26 — Pinning dell'indirizzo con httpx (anti DNS rebinding): si sostituisce l'host con l'IP GIA' validato e si conserva l'identita' vera con `headers={"Host": ...}` + `extensions={"sni_hostname": ...}`, cosi' la verifica del certificato resta sul nome. Il `Location` di un redirect si risolve sull'URL LOGICO, non su quello pinnato. Inoltre: `trust_env=False` (un HTTPS_PROXY azzera la denylist facendo risolvere il nome al proxy) e `Accept-Encoding: identity` (il cap conta i byte decodificati, quindi da solo non protegge dalla bomba zlib).
- 2026-07-26 — `caplog.text` e' reso con DEFAULT_LOG_FORMAT e gli attributi passati via `extra=` NON vi finiscono mai: asserire `"segreto" not in caplog.text` su un log che usa `extra` e' tautologico — passa anche loggando in chiaro E anche cancellando il log. Asserire su `caplog.records[i].<attributo>`, e in positivo sulla forma attesa.
- 2026-07-26 — `url_redatto` deve redigere anche la QUERY, non solo lo userinfo: negli export delle OTA il link *e'* la credenziale (`.../ical/<id>.ics?s=<segreto>`). Il path si tiene, serve a identificare il feed.
- 2026-07-26 — `alembic/env.py` con l'elenco dei modelli scritto a mano e' un cancello che tace: un modulo dimenticato non fa fallire nulla, `--autogenerate` semplicemente non vede le sue tabelle e propone `op.drop_table`. I modelli si SCOPRONO (`app/registro_modelli.py`, condiviso con le guardie). Verificare con `alembic check` dopo ogni migrazione nuova. Deriva preesistente nota e non mia: indici parziali di 0001 e `regime_lettura.host_id`.
- 2026-07-26 — Guardia che filtra per convenzione di nome (`endswith("Repository")`) e' cieca a un rename: verificare che i MODULI ci siano non basta, serve un pavimento sul numero di elementi effettivamente ispezionati. Vale per ogni meta-test costruito su un pattern di nomi.
- 2026-07-26 — Dopo aver aggiunto una dipendenza npm il lockfile va rigenerato con `npm install` e VERIFICATO con `npm ci` a `node_modules` cancellato: le opzionali multipiattaforma `@emnapi/*` restano disallineate e la CI cade su tutti i job che installano il frontend. Confermato di nuovo con `@testing-library/user-event`.
- 2026-07-26 — `@testing-library/user-event` e' ora installato (serviva per submit/reset/disabled sui form). Una callback passata a `mutate(..., {onSuccess})` invocata a mano nel test va dentro `act()`, altrimenti l'aggiornamento di stato non viene flushato. `input type="url"`: jsdom applica la sanitizzazione RFC, quindi il valore letto e' gia' senza spazi ai bordi.
- 2026-07-26 — Su una superficie che mostra stato, `isError` non e' opzionale: con `data` undefined e `isPending` false il ramo «lista vuota» afferma il falso («nessun calendario collegato» su una Struttura che ne ha tre). Stessa classe di danno di NFR-2. Distinguere sempre «non ce ne sono» da «non riusciamo a leggerli».
- 2026-07-26 — LEZIONE che vale oltre questo progetto: un insieme di CHECKPOINT non e' un bound sul tempo. `httpx.Timeout` e' per-operazione e si azzera a ogni byte; la fase di TESTA della risposta non passa da `iter_bytes`, quindi nessun controllo applicativo la vede. Un byte di intestazione ogni 9s dentro `read=10s` tiene la connessione per centinaia di ore (h11 concede 100 KiB di intestazioni). Il bound si mette su CIO' CHE ATTENDE: eseguire l'attesa in un worker e ABBANDONARLA al residuo del budget (`ThreadPoolExecutor` + `future.result(timeout=...)` + `shutdown(wait=False)`). I timeout per-operazione restano, limitati al residuo, solo per far morire in fretta il lavoro abbandonato.
- 2026-07-26 — Un controllo di sicurezza che sta su una riga ADIACENTE nel ciclo e' una riga cancellabile senza rompere nulla. Va FUSO nella funzione che produce il valore su cui opera: il divieto di declassamento `https -> http` sta dentro il calcolo del prossimo hop, quindi il prossimo hop non si calcola senza passare dal controllo. Regola generale: se una mutazione «cancella la chiamata» lascia la suite verde, il controllo e' nel posto sbagliato, non manca un test.
- 2026-07-26 — Un test che rilegge il PROPRIO file sorgente e asserisce la presenza di un letterale che sta sulla riga dell'assert non puo' fallire. Una guardia si verifica facendole ESAMINARE un caso costruito (file finto in `tmp_path`) e pretendendo che lo segnali — piu' il caso innocuo che non deve segnalare.
- 2026-07-26 — Una guardia che filtra per convenzione di nome va corretta togliendo il filtro, non aggiungendo un pavimento numerico: il pavimento va tenuto aggiornato a mano e con 12 contro 20 metodi servivano due rinomine per farlo mordere. Ispezionare OGNI classe pubblica del modulo rende la rinomina catturata per costruzione.
- 2026-07-26 — Una guardia i cui BERSAGLI si derivano dalla stessa funzione che sta testando e' autoreferenziale: aggiungere un modulo alla sua allowlist lo fa sparire dal controllo con tutti i test verdi. I bersagli si derivano dal FILESYSTEM (`app/*/models.py`, `__tablename__` letto con `ast`).
- 2026-07-26 — `Accept-Encoding: identity` e' una richiesta, non una garanzia: `iter_bytes()` decodifica in base al `Content-Encoding` della RISPOSTA. Un cap sui byte decodificati non limita la memoria — una bomba zlib da 400 KB arriva a 145 MiB — e un chunk che decodifica a vuoto non emette eventi, quindi salta anche i controlli nel ciclo. Va RIFIUTATA la risposta con codifica inattesa.
- 2026-07-26 — Pinning dell'indirizzo: `parti._replace(netloc=...)` sostituisce l'INTERO netloc e cancella lo userinfo, quindi httpx non deriva piu' il BasicAuth (un feed credenziato prende 401). E `parti.hostname` restituisce un IPv6 SENZA parentesi: nell'header `Host` RFC 9110 le vuole, e con la porta diventa impossibile da parsare. Conservare userinfo e mettere le quadre in entrambi i posti.
- 2026-07-26 — `pytest-timeout` va configurato GLOBALMENTE (`timeout = N` in `[tool.pytest.ini_options]`), non solo con un marker: e con `--strict-markers`, altrimenti se il pacchetto esce dal gruppo dev il marker degrada a warning e la protezione svanisce in silenzio.
- 2026-07-26 — Quando un test asserisce «quante volte e' successo X» al posto di «com'e' finito», e' fragile: nel test sul budget dei redirect il terzo hop poteva legittimamente partire (0,25+0,25 < 0,6) e l'assert `<= 2` cadeva a caso sotto carico. La proprieta' da asserire era l'ESITO — con budget per hop il fetch riesce, con budget totale no.
- 2026-07-26 — Un handler di test che scrive su una socket abbandonata dal client fa stampare a `socketserver` un traceback che sembra un errore del test. Avvolgere la risposta in `try/except OSError` e chiudere: nei test di scadenza l'abbandono e' l'esito ATTESO.
- 2026-07-26 — Spostare un dato di fixture nel posto giusto puo' rendere ROSSO un test che passava, e allora il test aveva un'asserzione falsa: le proprieta' iCal sconosciute il parser le RACCOGLIE (e' il normalizzatore a ignorarle), e l'assert «non compaiono» passava solo perche' la proprieta' stava a livello VCALENDAR, fuori dai VEVENT ispezionati.
