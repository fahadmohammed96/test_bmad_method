# Memorie — Winston — System Architect (Fase 3 Solutioning)

_Fatti durevoli e decisioni apprese durante il progetto HostPilot. Un fatto per voce, con data. Aggiornare via PR. Non duplicare `docs/project-context.md`._

<!-- Esempio:
- 2026-07-24 — <fatto appreso e perché conta>.
-->

- 2026-07-24 — Gli esiti espliciti delle [DECISIONE G2-A…E] del PRD non sono registrati nel repo (PRD/UX mergiati con frontmatter `draft`): l'architettura di Fase 3 è stata progettata parametrica rispetto a G2-A/B/D; gli esiti vanno fatti registrare al più tardi al gate G3. Conta perché ogni artefatto a valle rischia di poggiare su raccomandazioni scambiate per decisioni.
- 2026-07-24 — WS_ALLOGGIATI è un web service SOAP ufficiale della Polizia di Stato (manuale su questure.poliziadistato.it, endpoint Service.asmx, GenerateToken con WSKEY per account attivata dal portale): l'invio automatico Alloggiati Web ha un canale ufficiale — la scelta di tenerlo fast-follow è di rischio/legale, non di fattibilità tecnica.
- 2026-07-24 — Cassazione SS.UU. ord. n. 1527 del 23/01/2026 confermata su fonte primaria durante il reviewer gate: l'host è responsabile d'imposta per la tassa di soggiorno anche se l'ospite non paga; il Modello 21 è abolito, resta la dichiarazione annuale telematica all'AdE. Impatta il modello del registro tassa.
- 2026-07-24 — Il reviewer gate multi-lente (riconciliazione PRD/UX + rubrica + verifica web + attacco avversariale in subagent paralleli) ha trovato buchi reali che il drafting aveva mancato (archi del grafo dipendenze, ownership entità, lifecycle Prenotazione, semantica di cancellazione, pin di stack stantii): da rifare a ogni spine futuro, non è cerimonia.
- 2026-07-25 — L'uscita di rete del fetch iCal è governata da NFR-17 (PRD §6, ramo `docs/nfr17-politica-uscita-rete` di John) e dall'estensione di AD-4 nello spine: denylist come stato corrente; allowlist OTA e proxy di egress = opzioni aperte di Fahad (Deferred dello spine), non debito. Trappola: i due test design dell'Epic 2 ancorano l'SSRF a NFR-6, che nel PRD è la sicurezza GDPR dei documenti — il riferimento corretto è NFR-17. Ratifica formale di Fahad al rientro (hardening accolto dal supervisore su MYL-39/MYL-41).
- 2026-07-26 — Registrato **AD-21** (anagrafica `ospite`, decisione MYL-40 → PRD §14.2 in PR #38): retention propria per **azzeramento dei campi personali** — mai DELETE della riga —, decorrenza definita una sola volta in AD-21 (`check_out`, o uscita da `attiva` se precedente), parametro configurabile con proposta provvisoria 90 giorni in attesa di R-5 esteso. AD-20 ora elenca TRE cancellazioni distruttive ammesse. Conta perché la lista di AD-20 è dichiarata esaustiva: ogni futura decisione che introduce una distruzione di dato personale DEVE aggiornarla esplicitamente, o spine e AC si contraddicono in silenzio (classe di difetto "assenze" della retro Epic 1).
- 2026-07-30 — **AD-20 emendato: perimetro esplicito = dato di dominio; stato operativo del kernel fuori lista sotto quattro condizioni** (lettura chiesta da Fahad su PR #47, retention coda `job`). La lista esaustiva resta a TRE; `job`/`sessione`/`tentativo_login`/`outbox` consegnati sono lavoro, non fatti, e la loro purge non allunga la lista (condizioni: mai unica evidenza di un fatto; solo lavoro consumato; purge come job durevole con finestre in config; `failed` senza retention — deliberato, `last_error` è l'evidenza del guasto). Il precedente decisivo era GIÀ su `main` dall'Epic 1: `identity/jobs.py` cancella sessioni scadute e tracce login senza che nessuno abbia mai letto AD-20 come ostacolo. Lezione (da libreria): una lista esaustiva senza il perimetro del sostantivo che governa non è decidibile — due implementatori seri (Amelia, Murat) l'hanno letta in due modi opposti sulla stessa `DELETE`, entrambi ragionevolmente; il segnale d'ambiguità è due `DECISIONE-UMANA: sì` indipendenti sulla stessa domanda. **Round 2 (BOCCIA di Murat, corretto in `ea60c6c`), tre trappole del correttore:** (i) condizioni codificate da UN caso vanno verificate contro TUTTI i precedenti esistenti — la (3) scritta su `manutenzione.py` metteva fuori regola `identity/jobs.py`, cioè il precedente stesso su cui poggiava l'argomento (finestra derivata `login_finestra_minuti × 4` + parametri senza `gt=0`); (ii) curando un sostantivo senza perimetro ne avevo coniato un secondo senza perimetro nella stessa frase — l'elenco dello stato operativo era nato senza clausola di chiusura; (iii) le glosse sul dato personale perdono — «riguarda una persona» tirava dentro `sessione` (host_id) e `tentativo_login` (email): l'ancora giusta è «di cui il prodotto è registro», e il dato personale incidentale nello stato operativo si governa con la minimizzazione (NFR-11), non promuovendolo a dato di dominio.
- 2026-07-30 — **AD-20 terzo giro (MYL-66, F5/F7/F8): la cancellazione di stato operativo ha ora DUE forme, entrambe chiuse sullo stesso elenco di tabelle** — la purge (retention AUTOMATICA E CICLICA, governata dalle quattro condizioni) e l'operazione di ciclo di vita del meccanismo (logout, invalidazione a cambio password, azzeramento del debito del freno), vincolata alla sola (1). Trappola del round, la quarta della serie: curando un sostantivo senza perimetro («purge», dopo «dato di dominio» e «stato operativo») la tentazione è DEFINIRLO — ma la definizione lascia fuori ciò che non vi rientra; il pattern che chiude davvero è PARTIZIONARE lo spazio delle operazioni in forme nominate, tutte chiuse sullo stesso elenco, così che nessuna cancellazione sfugga rinominandosi (la clausola finale ora vieta «ogni cancellazione», non «la purge»). Le due decisioni di Fahad, lasciate aperte e decidibili nel testo, sono arrivate lo stesso giorno e sono ora REGISTRATE in AD-20: (a) le tracce `tentativo_login` SI CONSERVANO — evidenza di possibile attacco, sopravvivono all'accesso riuscito; il ciclo di vita azzera solo il DEBITO del freno, mai le righe; finestra di conservazione PROPRIA come parametro config validato all'avvio (AD-9), nessun numero nel testo (la tara il parere privacy R-5); story di codice su `azzera_per_email` instradata dal supervisore DOPO il merge dell'invariante — ordine invariante-prima, terza volta che funziona; (b) F6 `regime_lettura`: transizione tracciata nella forma di AD-19, la riga con `conteggio_confermato`/`confermato_il` resta — nessuna eccezione, nessun allargamento d'elenco, classificazione invariata; la #48 dichiara IN TESTA al corpo la dipendenza di merge dalla story di Amelia (stessa forma della #51). Trappola di coerenza del giro, in due tempi: registrare una decisione in fondo alla regola non basta — vanno ripassati (i) gli ESEMPI a monte che la contraddicono (la (2), l'illustrazione della (3), la glossa NFR-11 sul brute-force) e (ii) i SITI DI CODICE che la decisione ora governa, riletti sul bersaglio del merge, chiedendosi per ciascuno «con questo testo, l'implementatore in buona fede cambia il codice o no?»: al quarto giro (F10) la (3) ammetteva ancora la finestra derivata mentre l'invariante chiedeva un parametro proprio — due letture difendibili sulla stessa `DELETE` (`identity/jobs.py`: 4 × 15 = 60 minuti come «evidenza di possibile attacco») e con la lettura permissiva la decisione di Fahad restava sulla carta; chiuso dichiarando che per `tentativo_login` l'ammissione della (3) NON si applica. Un esempio o un'ammissione stantii dentro la stessa regola sono la contraddizione interna — la classe di difetto che l'emendamento esiste per spegnere.
- 2026-07-27 — **AD-21 esteso al `sommario` della Prenotazione** (decisione di Fahad su MYL-47) e **FR-20** registrata in architettura (MYL-42): l'azzeramento del `sommario` è la STESSA cancellazione ammessa da AD-20 estesa a un campo in più — la lista resta a tre. Trappola trovata registrando l'estensione, da distillare nella libreria di squadra alla retro Epic 2: l'upsert di sync riscrive `sommario` a ogni giro (`excluded.sommario` in `calendario/repository.py`), quindi senza la guardia «mai ripopolare un campo di una Prenotazione anonimizzata» un feed che conserva i VEVENT passati resusciterebbe il dato azzerato al sync successivo — la guardia è ora parte dell'invariante (stessa forma di `cessata_il`, che l'upsert già conserva); il codice è di Amelia nello stage dopo MYL-54, incluso il test di Story 2.3 che oggi pinna l'opposto (`test_retention_ospite.py`: il `sommario` sopravvive al job). Applicata anche la convenzione MYL-48: frasi normative citano l'insieme («tutti gli `AD-n` elencati nello spine»), gli intervalli numerati restano solo nelle fotografie datate (frontmatter, tabelle di copertura, note storiche).

