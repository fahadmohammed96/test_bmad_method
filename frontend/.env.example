# Contratto di configurazione del frontend (AR-11).
# Copia in `.env.local` per lo sviluppo locale. MAI committare `.env.local`.

# Base URL del backend HostPilot (il client generato la usa per /api/v1)
NEXT_PUBLIC_API_URL=http://localhost:8000
